import PreposicionesList from "./PreposicionesList";

type Props = {};
export default function Preposiciones({}: Props) {
  // ---- HOOKS
  // ---- FUNCTIONS
  // ---- JSX
  return (
    <main className="px-4 py-24">
      <PreposicionesList />
    </main>
  );
}
