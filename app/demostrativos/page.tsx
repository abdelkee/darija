import DemosList from "./DemosList";

type Props = {};
export default function Demostrativos({}: Props) {
  // ---- HOOKS
  // ---- FUNCTIONS
  // ---- JSX
  return (
    <main className="px-4 py-24">
      <DemosList />
    </main>
  );
}
